{
  "id": "m1-l10-errors",
  "title": "Errors and Exceptions",
  "sections": [
    {
      "type": "read",
      "contentMarkdown": "Errors stop programs. Use try/except to handle them."
    },
    {
      "type": "example",
      "starterCode": "try:\n    x = int('abc')\nexcept ValueError:\n    print('Invalid number!')",
      "explainMarkdown": "Catches a ValueError."
    },
    {
      "type": "exercise",
      "promptMarkdown": "Ask for a number and handle if the user enters text.",
      "starterCode": "# TODO: try-except around int(input())",
      "tests": [
        { "type": "includes", "match": "Invalid" }
      ],
      "hints": ["Wrap in try-except", "Catch ValueError"],
      "solutionCode": "try:\n    n = int(input('Number: '))\n    print(n)\nexcept ValueError:\n    print('Invalid number!')"
    },
    {
      "type": "quiz",
      "question": "Which keyword is used to handle exceptions?",
      "choices": ["try", "catch", "handle"],
      "answerIndex": 0,
      "explainMarkdown": "`try` with `except` handles exceptions."
    }
  ]
}
