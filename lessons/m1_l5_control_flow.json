{
  "id": "m1-l5-control-flow",
  "title": "Control Flow",
  "sections": [
    {
      "type": "read",
      "contentMarkdown": "Use `if`, `elif`, and `else` to make decisions in Python."
    },
    {
      "type": "example",
      "starterCode": "x = 10\nif x > 5:\n    print('Big')\nelse:\n    print('Small')",
      "explainMarkdown": "Checks if `x` is greater than 5."
    },
    {
      "type": "exercise",
      "promptMarkdown": "Ask the user for a number and print 'Even' if it's even, 'Odd' otherwise.",
      "starterCode": "# TODO: ask for number, check even/odd",
      "tests": [
        { "type": "includes", "match": "Even", "inputs": ["4"] }
      ],
      "hints": ["Use % 2 == 0", "if-else"],
      "solutionCode": "n = int(input('Number: '))\nif n % 2 == 0:\n    print('Even')\nelse:\n    print('Odd')"
    },
    {
      "type": "quiz",
      "question": "Which keyword is used for multiple conditions?",
      "choices": ["if", "elif", "else"],
      "answerIndex": 1,
      "explainMarkdown": "`elif` means 'else if'."
    }
  ]
}
